# NextRead — Frontend (Login)

This folder contains a minimal Vite + React frontend skeleton with a Login page that follows the provided Figma design.

Quick start (Windows PowerShell):

```powershell
cd "c:/Users/AIRON KIT ARTAZO/Desktop/React.Final/nextread"
# install
npm install
# run dev server
npm run dev
```

The login UI lives in `src/pages/Login.jsx` and styles are in `src/index.css`.

Next steps:
- Wire the login form to your backend API (fetch or axios).
- Add routing if you need multiple pages.

